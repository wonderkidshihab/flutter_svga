name: flutter_svga
description: "A Flutter package for rendering SVGA animations."
version: 0.0.1
homepage: https://github.com/5alafawyyy/flutter_svga

environment:
  sdk: ^3.6.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  archive: ^4.0.2
  http: ^1.3.0
  path_drawing: ^1.0.1
  protobuf: ^3.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

